import { MemberRegistration } from './member-registration.Model';

describe('MemberRegistration', () => {
  it('should create an instance', () => {
    expect(new MemberRegistration()).toBeTruthy();
  });
});
